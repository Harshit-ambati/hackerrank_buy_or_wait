# Model Usage Report

Pending the final online full-dataset run. `code/main.py` overwrites this file
with the actual provider names, model names, calls, and token counts. Configure
cost rates for the selected provider/model before preparing the final submission;
no API key or credential is ever written here.
