# Production is online-only. Copy this file to .env and set the key locally.
# The application loads .env without overriding variables already exported.
GEMINI_API_KEY=

GEMINI_MODEL=gemini-2.5-flash
AI_REQUEST_TIMEOUT_SECONDS=90
AI_MESSAGE_BATCH_SIZE=100
AI_MAX_RETRIES=4
